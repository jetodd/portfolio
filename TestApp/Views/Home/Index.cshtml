@model IEnumerable<TestApp.Models.Project>

@{
    ViewBag.Title = "Jess";
}
  
@for (int i = 0; i < @Model.Count(); i++)
{
    <h1>@Model.ElementAt(i).Name</h1>
    <p>@Model.ElementAt(i).Description</p>
}               